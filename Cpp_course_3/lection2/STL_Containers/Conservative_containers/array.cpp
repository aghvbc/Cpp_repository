#include <array>
#include <iostream>

int main(){
#include <array>
std::array<int, 5> a = {1, 2, 3, 4, 5}; // Размер 5
std::cout << "Size: " << a.size() << ", Element [2]: " << a[2] << "\n";
// a.push_back(6); // ОШИБКА! Нет такого метода
};
