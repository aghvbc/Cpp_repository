#include <iostream>
#include <string>


int main(){
    int K; std::cin >> K;
    std::string command;
    for (int i = 0; i < K; i++){
        std::cin >> command;

        if (command == "MOVE"){
            int n;
            std::cin>>n;
        }
        else if (command == "POSITION")
        {

        }
        else if (command == "COST")
        {

        }
        else if (command == "RESET")
        {

        }
    }
}